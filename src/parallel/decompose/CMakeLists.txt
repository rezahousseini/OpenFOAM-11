add_subdirectory( decompositionMethods )
if( ${METIS_FOUND} )
  add_subdirectory( metisDecomp )
endif()
if( ${PTSCOTCH_FOUND} )
  add_subdirectory( ptscotchDecomp )
  add_subdirectory( scotchDecomp )
endif()
if( ${ZOLTAN_FOUND} )
  add_subdirectory( zoltanDecomp )
endif()
